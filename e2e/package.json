{
  "name": "tv-invest-advisor-e2e",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "capture-demo": "playwright test --config=capture-demo.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
