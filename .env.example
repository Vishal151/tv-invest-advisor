# Used by docker-compose so the backend can write to bind-mounted chroma_db.
# Copy to .env in the repo root: cp .env.example .env
# On Linux/WSL, your uid/gid are usually 1000 (run: id -u && id -g)
UID=1000
GID=1000
